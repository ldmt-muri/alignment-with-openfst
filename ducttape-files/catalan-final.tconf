global {
    # Output directory:
    ducttape_output="/usr1/home/wammar/pos-runs/catalan-final"

    # general
    cores=28

    # data
    data_file="/usr1/home/wammar/pos-data/conll2007/autoencoder-format/catalan-cesscat-2007.tok"
    gold_file="/usr1/home/wammar/pos-data/conll2007/autoencoder-format/catalan-cesscat-2007.pos"
  
}

plan Full {
    reach Evaluate via (Model: autoencoder hmm) * (L2Strength: one ) * (L1Strength:  zero ) * (DirichletAlpha: point_nine) * (OptimizeLambdasFirst: yes) * (EmItercount: one) * (LbfgsItercount: one) * (TestWithCrfOnly: yes) * (MinRelativeDiff: point_o_o_one) 
}
